services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: sistemi_genit
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - genit_postgres:/var/lib/postgresql/data
volumes:
  genit_postgres:
