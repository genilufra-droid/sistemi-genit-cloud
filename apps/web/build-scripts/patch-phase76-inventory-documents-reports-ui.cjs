'use strict';
const fs=require('node:fs');
const path=require('node:path');
const root=path.resolve(__dirname,'..');
const htmlPath=path.join(root,'apps','web','index.html');
const jsPath=path.join(root,'apps','web','phase76-inventory-documents-reports-ui.js');
const start='<!-- SG_PHASE76_INVENTORY_DOCUMENTS_REPORTS_UI_PATCH_START -->';
const end='<!-- SG_PHASE76_INVENTORY_DOCUMENTS_REPORTS_UI_PATCH_END -->';
const esc=(value)=>value.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
let html=fs.readFileSync(htmlPath,'utf8');
const js=fs.readFileSync(jsPath,'utf8');
new Function(js);
html=html.replace(new RegExp(esc(start)+'[\\s\\S]*?'+esc(end)+'\\s*','g'),'');
const close=/<\/body>\s*<\/html>\s*$/i;
if(!close.test(html))throw new Error('Mungon mbyllja finale e HTML.');
html=html.replace(close,start+'\n<script id="sg-phase76-inventory-documents-reports-ui">\n'+js+'\n</script>\n'+end+'\n</body>\n</html>');
fs.writeFileSync(htmlPath,html);
const check=fs.readFileSync(htmlPath,'utf8');
if((check.match(/SG_PHASE76_INVENTORY_DOCUMENTS_REPORTS_UI_PATCH_START/g)||[]).length!==1)throw new Error('Patch-i Phase 7.6 nuk është idempotent.');
['SG_PHASE76_INVENTORY_DOCUMENTS_REPORTS_UI_START','20 Raportet e Magazinës','FLETË HYRJE','FLETË DALJE','Gjurmueshmëria Furnitor-Lot-Klient','sg76PrintDocument'].forEach((marker)=>{if(!check.includes(marker))throw new Error('Mungon '+marker);});
console.log('Phase 7.6 Inventory documents and reports UI injected.');
