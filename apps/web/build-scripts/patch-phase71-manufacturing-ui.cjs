'use strict';
const fs=require('node:fs');
const path=require('node:path');
const root=path.resolve(__dirname,'..');
const htmlPath=path.join(root,'apps','web','index.html');
const jsPath=path.join(root,'apps','web','phase71-manufacturing-ui.js');
const cssPath=path.join(root,'apps','web','phase71-manufacturing-ui.css');
const start='<!-- SG_PHASE71_MANUFACTURING_UI_PATCH_START -->';
const end='<!-- SG_PHASE71_MANUFACTURING_UI_PATCH_END -->';
const esc=(v)=>v.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
let html=fs.readFileSync(htmlPath,'utf8');
const js=fs.readFileSync(jsPath,'utf8');
const css=fs.readFileSync(cssPath,'utf8');
new Function(js);
html=html.replace(new RegExp(esc(start)+'[\\s\\S]*?'+esc(end)+'\\s*','g'),'');
const close=/<\/body>\s*<\/html>\s*$/i;
if(!close.test(html))throw new Error('Mungon mbyllja finale e HTML.');
const block=start+'\n<style id="sg-phase71-manufacturing-style">\n'+css+'\n</style>\n<script id="sg-phase71-manufacturing-ui">\n'+js+'\n</script>\n'+end+'\n';
html=html.replace(close,block+'</body>\n</html>');
fs.writeFileSync(htmlPath,html);
const check=fs.readFileSync(htmlPath,'utf8');
['SG_PHASE71_MANUFACTURING_UI_START','sg71-doc-link','sg71-smartbar','Mostrat e Klientit','Fushatat e Prodhimit','Urdhrat e Punës','Transferimet e Prodhimit','sg71OpenDocument'].forEach((m)=>{if(!check.includes(m))throw new Error('Mungon '+m);});
console.log('Phase 7.1 Manufacturing UI injected.');
