'use strict';
const fs=require('node:fs');
const path=require('node:path');
const root=path.resolve(__dirname,'..');
const webRoot=fs.existsSync(path.join(root,'apps','web','index.html'))?path.join(root,'apps','web'):root;
const htmlPath=path.join(webRoot,'index.html');
const jsPath=path.join(webRoot,'phase85-professional-document-templates.js');
const start='<!-- SG_PHASE85_PROFESSIONAL_DOCUMENT_TEMPLATES_PATCH_START -->';
const end='<!-- SG_PHASE85_PROFESSIONAL_DOCUMENT_TEMPLATES_PATCH_END -->';
const esc=v=>v.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
if(!fs.existsSync(htmlPath))throw new Error('Mungon index.html për Phase 8.5: '+htmlPath);
if(!fs.existsSync(jsPath))throw new Error('Mungon phase85-professional-document-templates.js: '+jsPath);
let html=fs.readFileSync(htmlPath,'utf8');
const js=fs.readFileSync(jsPath,'utf8');
new Function(js);
html=html.replace(new RegExp(esc(start)+'[\\s\\S]*?'+esc(end)+'\\s*','g'),'');
const close=/<\/body>\s*<\/html>\s*$/i;
if(!close.test(html))throw new Error('Mungon mbyllja finale e HTML.');
html=html.replace(close,start+'\n<script id="sg-phase85-professional-document-templates">\n'+js+'\n</script>\n'+end+'\n</body>\n</html>');
fs.writeFileSync(htmlPath,html);
const check=fs.readFileSync(htmlPath,'utf8');
if((check.match(/SG_PHASE85_PROFESSIONAL_DOCUMENT_TEMPLATES_PATCH_START/g)||[]).length!==1)throw new Error('Patch-i Phase 8.5 nuk është idempotent.');
['SG_PHASE85_PROFESSIONAL_DOCUMENT_TEMPLATES_START','SG85_INVOICE_PURCHASE','SG85_INVOICE_SALE','SG85_CASH_RECEIPT','SG85_CASH_PAYMENT','SG85_GOODS_RECEIPT','SG85_GOODS_ISSUE'].forEach(m=>{if(!check.includes(m))throw new Error('Mungon '+m);});
console.log('Phase 8.5 professional document templates injected.');
